# Tradier sandbox API token — used by both stdio and Worker entrypoints.
TRADIER_TOKEN=your_tradier_sandbox_token_here

# Shared bearer token that clients must send in Authorization: Bearer <value>
# Used only by the Worker entrypoint. Generate with:
#   openssl rand -hex 32
MCP_AUTH_TOKEN=generate_a_long_random_string
